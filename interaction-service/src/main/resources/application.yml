
spring:
    application:
        name: interaction-service
    config:
        import: nacos:interaction-service-dev.yml
    cloud:
        nacos:
            discovery:
                server-addr: localhost:8848
            config:
                server-addr: localhost:8848
                file-extension: yaml